#include <stdio.h>

int main(){
/*
variables en C

las variables son contenedores para almacenar valores de datos,como numeros y caracteres.

en C hay diferentes tipos de variables (definidas por diferentes palabras claves),por ejemplo:

    +int/almacena enteros (numeros enteros),sin decimales,como 123 o -123

    *float - almacena numeros de coma flotante o decimales,como 19.99 o -19.19

    +char -almacena caracteres individuales,como A o B.los valores de char estan entre comillas simples
*/

/*
    declaracion (Crear) variables

    para crear una variable en C,esoecifique el tipo y asignele un valor:

    *sintaxis

    tipo de nombreVariable = valor;

    donde tipo es uno de los tipos de variables de C (como int) y nombreVariable es el nombre de la variable (como x miNombre). El signo igual se ultiliza para asignar un valor de una variable.
    */

    // crear una variable que almacene un numero entero

     int miNumero = 15;



        return 0;
}


    /*
    #include <stdio.h>
    
    es una libreria de archivos de header que nos permite trabajar con funciones de entrada y salida,como printf()(que se encuentra en la linea 4) los archivos de encabezado agreagan funcionalidad a los porgramas C.
    */